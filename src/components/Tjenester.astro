---
import {getCollection} from 'astro:content';
import Layout from '../layouts/Layout.astro';

// 1. Hent alle filene fra src/content/tjenester/
const allServices = await getCollection('tjenester');

// 2. Sorter dem alfabetisk basert på tittelen i frontmatter (nb = norsk)
const sortedServices = allServices.sort((a, b) =>
    a.data.title.localeCompare(b.data.title, 'nb')
);
---
<section id="tjenester" class="scroll-mt-6 bg-white py-16 md:py-24 pb-0">
    <div class="container mx-auto px-4 mb0">

        {/* Overskrift - Sticky på mobil, vanlig på desktop */}
        <div
                style="top: var(--nav-total-height, 4rem);"
                class="sticky md:relative top-auto z-30 bg-white/95 md:bg-transparent py-8 mb-12 text-center"
        >
            <h2 class="text-4xl md:text-5xl font-black text-slate-900 italic tracking-tight">
                Våre Tjenester
            </h2>
            <div class="w-20 h-1 bg-blue-600 mx-auto mt-4"></div>
        </div>

        <div class="flex flex-col md:grid md:grid-cols-2 lg:grid-cols-3 gap-6 md:gap-8">
            {sortedServices.map((service, index) => (
                /* Vi sender kun index som en variabel, ingen direkte 'top' her */
                    <div
                            style={`--i: ${index};`}
                            class="stack-card sticky md:relative group bg-white p-8 rounded-3xl shadow-[0_10px_40px_rgba(0,0,0,0.04)] border border-slate-100 flex flex-col mb-[10vh] md:mb-0 min-h-[300px] overflow-hidden transition-all duration-300"
                    >
                        {/* Blå detalj - Nå trygt innenfor overflow-hidden */}
                        <div class="absolute top-0 right-0 w-20 h-20 bg-blue-50 rounded-bl-full -mr-8 -mt-8 transition-colors group-hover:bg-blue-100"></div>

                        <h3 class="font-bold text-2xl mb-4 text-slate-900 group-hover:text-blue-600 transition-colors relative z-10">
                            <a href={`/tjenester/${service.slug}`}>{service.data.title}</a>
                        </h3>

                        <div class="relative z-10 flex-grow">
                            <p class="text-slate-600 text-sm leading-relaxed line-clamp-3 mb-8">
                                {service.data.ingress}
                            </p>
                        </div>

                        <div class="mt-auto relative z-10 flex items-center text-blue-600 font-bold text-xs uppercase tracking-widest">
                            Les mer <span class="ml-2">→</span>
                        </div>

                        {/* Progressbar-detalj i bunn */}
                        <div class="absolute bottom-0 left-0 h-1.5 bg-blue-600 w-0 group-hover:w-full transition-all duration-500"></div>
                    </div>
            ))}
        </div>
    </div>
</section>

<style>
    /* MOBIL: Her aktiverer vi stacking-effekten */
    @media (max-width: 767px) {
        .stack-card {
            /* Bruker variabelen --i som vi satte i loopen */
            top: calc(var(--nav-total-height, 4rem) + 160px + (var(--i) * 4px));
            z-index: var(--i);
        }
    }

    /* DESKTOP: Her tvinger vi alt tilbake på plass */
    @media (min-width: 768px) {
        /* 1. Fix for at overskriften ikke skal dekke navbaren */
        .sticky {
            position: relative !important;
            top: 0 !important;
            z-index: 10 !important;
        }

        /* 2. Fix for kortene i rutenettet */
        .stack-card {
            position: relative !important;
            top: 0 !important;
            z-index: 1 !important;
            margin-bottom: 0 !important;
            box-shadow: 0 4px 6px -1px rgb(0 0 0 / 0.1) !important;
        }
    }

    /* Felles fiks for bølgen */
    svg {
        display: block;
    }
</style>